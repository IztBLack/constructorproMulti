# Copia este archivo a `.env.local` y rellena los valores.
# `.env.local` está en .gitignore: nunca lo subas.

# ── Supabase (obligatorias) ─────────────────────────────────────────────────
# Las dos son públicas por diseño: la protección real es la RLS de Postgres.
# La `service_role` NO va aquí ni en ningún otro archivo del repo.
NEXT_PUBLIC_SUPABASE_URL=https://tu-proyecto.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ── Sentry (opcional) ───────────────────────────────────────────────────────
# Sin esta variable el reporte de errores queda APAGADO y la app funciona igual;
# no hace falta cuenta de Sentry para clonar, correr ni desplegar el proyecto.
# El DSN es público (solo permite escribir eventos). Ver
# `src/lib/observabilidad/sentry.ts`.
#
# El móvil usa un proyecto de Sentry APARTE (plataforma Flutter) y su DSN se
# compila dentro del APK. `build_release.ps1` lo toma de `SENTRY_DSN` en
# `.env.tokens` (en la raíz del repo, fuera de git); exportar
# `$env:SENTRY_DSN` a mano sigue funcionando y tiene prioridad.
NEXT_PUBLIC_SENTRY_DSN=

# ── PDF en local (opcional) ─────────────────────────────────────────────────
# En Vercel el Chromium que imprime los PDF lo pone `@sparticuz/chromium`. En tu
# máquina ese binario no sirve (es de Linux), así que `render-html-to-pdf.ts`
# busca el Chrome o Edge instalado. Define esta variable solo si el tuyo está en
# una ruta fuera de lo común.
#   CHROME_PATH=C:\Program Files\Google\Chrome\Application\chrome.exe
CHROME_PATH=
